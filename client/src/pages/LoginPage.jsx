function LoginPage() {
    return (
        <main>
            <h1>Login Page</h1>
            <p>Login form implementation is the next milestone.</p>
        </main>
    );
}

export default LoginPage;